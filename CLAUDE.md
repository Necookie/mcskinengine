Before starting any task, check the Skill Routing Rules below and proactively invoke the Skill tool for every skill that matches — don't wait to be asked. Multiple skills can apply to one task (e.g. a Next.js route with UI polish loads both `nextjs-app-router-patterns` and `i-impeccable`).

@AGENTS.md
